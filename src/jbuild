(jbuild_version 1)

(executable
  ((name main)
   (public_name opam-nixify)
   (package opam-nixify)
   (modules (main))
   (flags (:standard))
   (libraries (opam-client opam-state opam-solver re cmdliner))))
